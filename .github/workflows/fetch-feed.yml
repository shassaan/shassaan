name: fetch-feed

on:
  schedule:
    # Once a day at 8 AM
    - cron: 28 12 * * *

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: JasonEtco/rss-to-readme@v1
        with:
          feed-url: https://shassaan.medium.com/feed
          readme-section: feed
